define(function () {

    // this is a module
    // this needs to be a file, and use the define method


    return {
        info: function (message) {
            console.info(message);
        }
    }
});